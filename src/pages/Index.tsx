import FlowBuilder from '../components/FlowBuilder';

/**
 * Index Page - Main entry point for the Chatbot Flow Builder
 * Features:
 * - Full-screen flow builder interface
 * - Drag and drop node creation
 * - Visual flow editing with validation
 * - Settings panel for node configuration
 */
const Index = () => {
  return <FlowBuilder />;
};

export default Index;
